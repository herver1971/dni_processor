# ============================================================
# DNI Processor — Dependencies (v0.3.0a: backend FastAPI)
# ============================================================

# --- Web framework ---
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
python-multipart>=0.0.9,<1.0       # Multipart uploads
jinja2>=3.1,<4.0                   # Templates HTML

# --- Visión por computadora ---
opencv-python-headless>=4.9,<5.0
numpy>=1.26,<2.0
Pillow>=10.2,<12.0
pillow-heif>=0.15,<1.0             # Soporte HEIC para fotos de iPhone

# --- OCR (sugiere matcheos en Sprint 3) ---
easyocr>=1.7,<2.0

# --- PDF ---
fpdf2>=2.7,<3.0

# --- Matching y utilidades ---
python-Levenshtein>=0.25,<1.0

# --- Validación y settings ---
pydantic>=2.5,<3.0
pydantic-settings>=2.1,<3.0

# --- Hardening (rate limiting) ---
slowapi>=0.1.9,<1.0

# --- CLI (todavía usado por scripts/) ---
typer>=0.12,<1.0
rich>=13.7,<14.0

# --- Testing ---
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0
httpx>=0.27,<1.0                   # TestClient de FastAPI
